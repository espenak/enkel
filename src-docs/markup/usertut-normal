<?xml version="1.0" encoding="utf-8"?>
<post xmlns="http://enkel.wsgi.net/xml/staticcms">
	<summary>
		When EXML markup is used to edit wiki-/blog-/cms- like
		articles you will probably use the "language" introduced
		here to edit the "body" text of the article.
	</summary>
	<tag>Introduction</tag>
	<section xmlns="http://enkel.wsgi.net/xml/markup"
			xmlns:s="http://enkel.wsgi.net/xml/staticcms">
		<h>User tutorial 1 (not finished)</h>
	
		<p>You should read  <a s:post="usertut-xml">User tutorial
		- XML</a> before this tutorial.</p>

		<p>The XML tutorial uses the EXML markup language as
		an example, so you should be somewhat familiar with
		it by now.</p>
	</section>
</post>
